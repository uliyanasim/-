#include <stdio.h>
int main() {
 
  int i;
  int sum=0;

  double a[6] = { 5, 4, 3, 2, 1, 6 }; 
  while (i < 6)    
  {
    sum = sum + i+1; 
    i++;           
  }
  printf("sum = %d\n", sum); 
  for (i = 0; i<6;++i)
    {
    a[i]/=sum;
    printf("%f ", a[i]);
    }  
}
